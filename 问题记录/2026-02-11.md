- 问题记录
  - 访问traefik 报no available server
  - 配置dashboard的ingressroutes后无法访问
- 排查问题
  - 去除middleware插件后，访问正常
  - 检查kubectl api-versions，发现traefik的api版本为traefik.containo.us/v1alpha1
* Helm 默认不会升级已存在的 CRD，需要显式开启或手动处理。
  ```bash
  helm upgrade --install traefik traefik/traefik \
    --namespace traefik --create-namespace \
    --set providers.kubernetesCRD.enabled=true
  ```
* 清理旧版的CRD
  ```bash
  for i in `kubectl get crd|grep traefik`
  do
  kubectl delete crd $i
  done
  ```